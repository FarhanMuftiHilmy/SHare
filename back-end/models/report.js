class Reports {
    constructor(id, userReport, titleReport, Report, createdReportby ) {
            this.id = id;
            this.userReport = userReport;
            this.titleReport = titleReport;
            this.report = report;
            this.createdReportby = createdReportby;
    }
}

module.exports = Report;