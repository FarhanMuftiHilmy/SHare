class Resume {
    constructor(id, userResume, titleResume, resume, createdResumeby ) {
            this.id = id;
            this.userResume = userResume;
            this.titleResume = titleResume;
            this.resume = resume;
            this.createdResumeby = createdResumeby;
    }
}

module.exports = Resume;